const welcome = (number, groupname) => {
    return `*_🌚En nombre de todos los integrantes de este increible grupo🍭 @${number} Te damos la Bienvenida a ${groupname} Espero y sea de su agrado🧘🏻‍♂️ ,contamos con su apoyo🔥 lee la descripción para evitar ser eliminado (🐤SOY TU ASISTENTE VIRTUAL, UN HUMILDE BOT ESCRIBE: !menu para ver mis comandos✨)_*`
}
exports.welcome = welcome

const bye = (number) => {
    return `_😹Finalmente la carga del grupo se redujo en -1 *''Adiós''* @${number}⁩ No te extrañaremos😆, el próximo en salir será kuakiado ok.no_`
}
exports.bye = bye
